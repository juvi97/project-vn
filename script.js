define(['./globalState', './gameState'], function (globalState, gameState) {
  'use strict';
  var exports = {};
  
  function loadSection(scriptID, index) {
    index = index || 0;
    
    
  }
  
  
  return exports;
});